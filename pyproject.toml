[project]
name = "last-war-vs-parser"
version = "0.1.0"
description = "Local extraction and review of alliance-member VS rankings from iOS recordings"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

